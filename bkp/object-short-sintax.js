const nome = "Maria Eduarda";
const alfabetizada = true;

let pessoa = {
    nome,
    idade: 18,
    alfabetizada
};

console.log(pessoa);

/*Object short sintax basicamente é um modo "curto" de 
construir a sintaxe do objeto, caso já tenha uma constante 
com o mesmo nome, basta colocar o atributo, sem o valor.*/ 

